# Portafolio Personal — Práctica 1 (Vanilla)

**Estado: plantilla sin completar.** Este esqueleto tiene la estructura, el CSS
responsive y la lógica JS de ejemplo, pero todo el contenido marcado con
`[placeholder]` o `TODO` lo tienes que reemplazar tú con tus datos reales
antes de subirlo — así lo exige la rúbrica de la práctica.

## Qué falta hacer (checklist)

- [ ] `index.html`: reemplazar `[Nombre]`, `[Tu carrera / rol]`, párrafos de
      "Sobre mí" y habilidades reales.
- [ ] `assets/`: agregar tu foto de perfil como `foto-perfil.jpg` (y las
      imágenes de tus proyectos si las usas).
- [ ] `script.js`: reemplazar los 4 objetos del array `proyectos` con tus
      proyectos reales (título, tecnologías, descripción).
- [ ] `styles.css`: agregar los estilos que falten (`.card-proyecto`,
      `.lista-habilidades`, `form`, `footer`) — el archivo solo trae el
      layout base y el navbar.
- [ ] Revisar accesibilidad: `alt` en imágenes, contraste de colores,
      `:focus-visible` en enlaces y botones.
- [ ] Probar responsive en 320px, 768px y 1024px (DevTools → device toolbar).
- [ ] Escribir este mismo README con la descripción real de tu proyecto.

## Estructura

```
mi-portafolio/
├── index.html
├── styles.css
├── script.js
├── README.md
└── assets/
    └── (tus imágenes van aquí)
```

## Cómo probarlo

Abre `index.html` directamente en el navegador, o usa la extensión
"Live Server" de VS Code para recarga automática.
